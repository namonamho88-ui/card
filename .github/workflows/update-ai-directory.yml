name: 🤖 Update AI Directory Data

on:
  schedule:
    # 매일 한국 시간 00:00 (UTC 15:00)
    - cron: '0 15 * * *'
  workflow_dispatch: # 수동 실행 가능

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🚀 Fetch AI Directory data from HuggingFace
        run: node scripts/fetchAIDirectory.mjs

      - name: 📝 Commit and Push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/data/aiDirectory.json
          # 변경 사항이 있을 때만 커밋
          git diff --staged --quiet || git commit -m "🤖 auto: AI Directory 데이터 업데이트 $(date +'%Y-%m-%d %H:%M KST' -d '+9 hours')"
          git push
